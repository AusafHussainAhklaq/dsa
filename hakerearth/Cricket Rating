t=int(input())
arr=list(map(int,input().split()))
mmax=0
total_max=0
for i in range(len(arr)):
    mmax+=arr[i]
    if mmax<=0:
        mmax=0
    z=max(mmax,total_max)
    total_max=z
print(total_max)
